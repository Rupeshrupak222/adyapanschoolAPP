<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Only trust system CAs — do NOT trust user-installed certificates -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
